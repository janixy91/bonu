{
  "engines": {
    "node": "18.x",
    "npm": "9.x"
  },
  "scripts": {
    "heroku-postbuild": "cd $PROJECT_PATH && npm install && npm run build",
    "start": "cd $PROJECT_PATH && npm start"
  }
}